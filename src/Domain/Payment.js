import React, { useEffect } from 'react'

function Payment() {
    
    useEffect(() => {
        window.scrollTo(0, 0);
      }, []);

  return (
    <div>
        <h1>Payment process</h1>
    </div>
  )
}

export default Payment